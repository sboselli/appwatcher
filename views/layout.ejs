<%- (locals.head) ? include(head) : include('head') -%>
<%- (locals.header) ? include(header) : include('header') -%>

<%- include(body) -%>

<%- (locals.footer) ? include(footer) : include('footer') -%>